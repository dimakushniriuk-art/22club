# Indice route (App Router)

- **Lista file:** `audit/routes_files_clean.txt` (~99 `page.tsx` + layout principali).
- **Staff:** prefisso `/dashboard/…` (clienti, calendario, documenti, pagamenti, chat, marketing, admin, nutrizionista, massaggiatore, …).
- **Atleta:** prefisso `/home/…` (allenamenti, chat, documenti, profilo, progressi, …).
- **Pubbliche / auth:** `/`, `/login`, `/forgot-password`, `/reset-password`, `/design-system`, `/welcome`.

Mappa URL esatta: derivare path da struttura `src/app/.../page.tsx` (segmenti = URL).
