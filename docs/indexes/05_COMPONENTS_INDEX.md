# Indice componenti

- **Lista completa:** `audit/components_clean.txt`
- **Macro-cartelle:**
  - `src/components/ui/` — primitivi
  - `src/components/dashboard/` — staff (admin, documenti, pagamenti, marketing, …)
  - `src/components/athlete/` — home atleta
  - `src/components/calendar/`, `appointments/`, `chat/`, `communications/`

Barrel `index.ts` dove presenti (es. `appointments`, `calendar`, `chat`).
