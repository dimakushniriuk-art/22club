/**
 * Tipi DB: fonte unica da schema generato (evita drift con duplicati manuali).
 */
export type { Json, TablesInsert, TablesUpdate, Tables, Database } from '@/lib/supabase/types'

export type { Database as SupabaseDatabase } from '@/lib/supabase/types'
