import { StaffAthleteSegmentSkeleton } from '@/components/layout/route-loading-skeletons'

/** Navigazione verso scheda atleta e sottopagine progressi (stesso segmento `[id]`). */
export default function DashboardAtletaIdLoading() {
  return <StaffAthleteSegmentSkeleton />
}
