import { StaffDashboardSegmentSkeleton } from '@/components/layout/route-loading-skeletons'

/** Navigazione nel segmento `/dashboard/massaggiatore/*` (stesso guscio del dashboard staff). */
export default function MassaggiatoreSegmentLoading() {
  return <StaffDashboardSegmentSkeleton />
}
