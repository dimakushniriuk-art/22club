import { StaffDashboardSegmentSkeleton } from '@/components/layout/route-loading-skeletons'

/** Navigazione nel segmento `/dashboard/nutrizionista/*` (stesso guscio del dashboard staff). */
export default function NutrizionistaSegmentLoading() {
  return <StaffDashboardSegmentSkeleton />
}
