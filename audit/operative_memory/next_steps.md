# Prossimi step

1. Approvare batch A (fix `documenti/page.tsx`).
2. Eseguire `next build` / `tsc`; annotare altri errori TS se presenti.
3. Passata lint mirata; batch lint 3–5 file.
4. RBAC: incrociare `auth_roles_map_clean` con middleware.
5. Valutare `api/debug-trainer-visibility`.
