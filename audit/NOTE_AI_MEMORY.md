# Nota su ai_memory

Le regole progetto (`.cursor/rules/22club-project-rules.mdc`) vietano la modifica della cartella `ai_memory/`.

Il contenuto richiesto per `ai_memory/*.md` è stato generato in **`audit/operative_memory/`** (stessi nomi file). Copiare manualmente in `ai_memory/` se serve aggiornare quella cartella fuori dall’agent.
