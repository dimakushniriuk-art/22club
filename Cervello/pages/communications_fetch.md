- communications_fetch
	- ATOMS
		- source_file=src/lib/communications/service.ts
		- source_file=src/lib/communications/recipients.ts
		- tables=communications communication_recipients profiles user_push_tokens
		- org_helper=ref=@/lib/organizations/current-org getCurrentOrgIdFromProfile (recipients filter.org_id path)
		- chunk_helper=ref=@/lib/supabase/in-query-chunks chunkForSupabaseIn
		- recipient_user_id_maps_profile_id=true (Recipient.user_id → recipient_profile_id in insert ref=[[communications_mutations]])
		- api_route_list=src/app/api/communications/list/route.ts
		- api_route_recipients=src/app/api/communications/recipients/route.ts
		- api_route_list_athletes=src/app/api/communications/list-athletes/route.ts
		- api_route_recipients_count=src/app/api/communications/recipients/count/route.ts
	- COMPRESSED
		- getCommunications=count `communications` head estimated + select `*` order `created_at` desc; filter `status` OR `statuses` IN (statuses wins); `type` filter skipped when options.type===`all`; `created_by_profile_id`; `limit`+`range(offset,offset+limit-1)` optional
		- getCommunicationById=`communications` single by id + `communication_recipients` `*` by `communication_id` order `created_at` desc → merge `CommunicationWithStats`
		- getRecipientsByFilter=empty guard se nessun all_users/role/athlete_ids; role|all_users senza orgId→[]; `profiles` select id user_id email telefono role `stato` attivo + org eq + role eq (athlete literal) + `in(id)` chunked merge Map; opzionale `user_push_tokens` active per `has_push_token` (warn on token error)
		- countRecipientsByFilter=type not email|all→count 0; stesse guard org/empty; `profiles` count exact head `email` neq `''`; athlete_ids sum counts per chunk; in-memory cache TTL 60s key filter+communicationType; `setInterval` cleanup cache 2min
		- validateRecipients=per recipient email required se type email|all → valid|invalid|errors[]
		- generateRecipientTypes=email|all only → rows `{user_id,recipient_type:email}` solo se recipient.email ref=[[communications_mutations]] createCommunicationRecipients
		- email_html_static=generateEmailHTML wrapEmailLayoutDark EMAIL_DESIGN getEmbeddedLogoDataUri ref=[[communications_context]] template_stack
		- ui_template_preview_delta=src/app/dashboard/comunicazioni/template/page.tsx import generateEmailHTML solo client useMemo—nessuna chiamata API communications; anteprima iframe srcDoc; metadata.email_template da customHtml|DEFAULT coerente con applyPlaceholders ref=email_html_static (delta=solo wiring UI staff)
		- api_get_list=GET `getCommunications` ref=service lib; query `status` CSV→`statuses` whitelist draft|scheduled|sending|sent|failed|cancelled; `type` push|email|sms|all; `limit` 1–100 `offset`≥0; JSON `{communications,count}` senza chiave top-level `data` ref=compat apiGet
		- api_get_recipients=GET `communication_id` required; session select recipient rows order `created_at` asc; `profiles` `in(id)` chunked nome merge first/last vs nome/cognome vs email; response `{recipients}` ref=[[communications_context]] api_recipients_auth_surface
		- api_get_list_athletes=GET session `profiles` eq org caller eq role athlete eq stato attivo; `{athletes:id,name,email}` displayName merge
		- api_get_recipients_count=GET `countRecipientsByFilter` ref=lib recipients; `org_id` da profilo session+getCurrentOrgIdFromProfile; query `role` `type` default email; `all_users` 1|true o default se nessun filtro esplicito backward compat; `athlete_ids` CSV→array; error lib→`{count:0}` 200 ref=[[communications_context]] api_count_silent_fail
		- ui_orchestration_count_params=useCommunicationsPage costruisce recipients/count con type=formType + all_users|role=athlete|athlete_ids da formRecipientFilter ref=[[communications_modal]] new_modal_recipients_ui
	- QUERIES
		- use=(none) template page preview zero network fetch dominio
		- use=communications.select * count head estimated
		- use=communications.select * single eq id
		- use=communication_recipients.select * eq communication_id order created_at
		- use=profiles.select id user_id email telefono role eq stato attivo eq org_id optional eq role optional in id
		- use=user_push_tokens.select user_id in user_id eq is_active true
		- use=profiles.select user_id count exact head eq stato neq email empty (+ stessi filtri countRecipientsByFilter)
		- use=communication_recipients.select fields eq communication_id order created_at asc (api recipients)
		- use=profiles.select id user_id email first_name last_name nome cognome phone telefono in id (api recipients join)
		- use=profiles.select id nome cognome first_name last_name email eq org_id eq role athlete eq stato attivo (api list-athletes)
	- CONTEXT
		- name=estimated_count
		- issues=getCommunications count mode `estimated` vs exact business need ref=service.ts
		- name=list_admin_vs_send_session
		- issues=GET list chiama `getCommunications` (admin lib) senza `created_by_profile_id` in call API—scope globale DB mitigato solo da gate ruolo route ref=[[communications_context]] api_list_scope
		- name=ui_list_fetch_delta
		- issues=useCommunications lista via apiGet stesso contract ref=api_get_list; fallback supabase client `count exact`+range/limit—count può divergere da estimated server ref=estimated_count
		- name=ui_single_fetch_delta
		- issues=useCommunications fetchCommunicationById solo supabase client join recipients order created_at desc (non route detail dedicata)
