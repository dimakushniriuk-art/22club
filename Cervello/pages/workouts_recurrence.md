- workouts_recurrence
	- ATOMS
		- WKT.rec.field=sessions_until_refresh su workout_days|null se non finito|≥1|persistenza Math.min(Math.floor(sur),999) in create|update|duplicate source copy
		- WKT.rec.preview.wiz=buildDaysSessionsPreviewFromWizard(days)→day_number|title|sessions_until_refresh|sessions_completed_count=0
		- WKT.rec.preview.dup=buildDaysSessionsPreviewFromDuplicateSource(sourceDays)→stesso shape
		- WKT.rec.counts.lista=useWorkoutPlans lista aggrega workout_logs per workout_day_id completed filtro atleta plan match→sessions_completed_count ref=[[workouts_fetch]] WKT.wp.list.daysPreview
	- COMPRESSED
		- non è RRULE calendario; è “ciclo sessioni per giorno scheda” + conteggio completamenti da workout_logs
	- QUERIES
		- use=src/hooks/workout-plans/use-workout-plans.ts#buildDaysSessionsPreviewFromWizard|buildDaysSessionsPreviewFromDuplicateSource|sessions_until_refresh handling
	- CONTEXT
		- nome:rec-vs-calendar
		- issues=modulo separato da workouts_calendar (non presente); solo semantica refresh/sessioni
		- use=[[workouts_context]]
