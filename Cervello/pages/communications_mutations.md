- communications_mutations
  - ATOMS
    - source_file=src/lib/communications/service.ts
    - source_file=src/lib/communications/email.ts
    - source_file=src/lib/communications/email-batch-processor.ts
    - source_file=src/lib/communications/email-resend-client.ts
    - source_file=src/lib/communications/scheduler.ts
    - source_file=src/lib/communications/send-reset-password-email.ts
    - admin_client=createAdminClient lazy singleton cast SupabaseClient (pattern condiviso col folder)
    - email_batch_size=100
    - email_batch_delay_ms=2000
    - tables=communications communication_recipients profiles
    - api_route_send=src/app/api/communications/send/route.ts
    - api_route_send_email_to_athlete=src/app/api/communications/send-email-to-athlete/route.ts
    - api_route_check_stuck=src/app/api/communications/check-stuck/route.ts
  - COMPRESSED
    - createCommunication=insert communications created_by_profile_id title message type status draft|scheduled da scheduled_for recipient_filter json metadata counters zero ref=[[communications_fetch]] CreateCommunicationInput
    - updateCommunication=patch fields; se recipient_filter cambiato vs JSON stringify precedente e status in draft|scheduled→delete all `communication_recipients` eq communication_id; poi update incluso total_recipients=0 quando filter cambia ref=[[communications_context]] filter_change_delete
    - deleteCommunication=delete communications eq id (cascade recipients DB-side assumption)
    - createCommunicationRecipients=insert rows communication_id recipient_profile_id recipient_type email status pending chunked sequential chunkForSupabaseIn; fail chunk→return error prior chunks remain ref=[[communications_context]] chunk_insert_atomicity
    - updateRecipientStatus=patch status sent_at delivered_at opened_at failed_at error_message metadata json
    - updateCommunicationStats=aggregate counts da recipients status sent|delivered|opened|failed|bounced→update communications totals
    - sendCommunicationEmail=load comm by id; guard type email|all else error; update status sending; select recipients pending email; empty→updateCommunicationStats then status failed+sent_at; else generateEmailHTML metadata logo_url fallback getEmbeddedLogoDataUri ref=[[communications_fetch]]; processEmailBatches; updateCommunicationStats; final status sent se almeno un sent altrimenti failed (tutti failed) + sent_at; catch→status failed
    - sendEmailToRecipient=load recipient+joined communications; profile email by user_id (!) eq su profiles—mismatch note ref=[[communications_context]] profile_lookup_single_retry; fail email→updateRecipientStatus failed; sendEmailViaResend success→sent+stats update
    - processEmailBatch=parallel map recipients profile by recipient_profile_id sendEmailViaResend updateRecipientStatus per row ref=email-resend-client
    - processEmailBatches=slice batches size 100 sequential processEmailBatch delay 2s tra batch
    - processScheduledCommunications=select communications status scheduled scheduled_for lte now not null; per row ensureRecipientsCreated ref=scheduler then switch type email|all→sendCommunicationEmail; push|sms→skip logged unsupported; default throw unknown type
    - ensureRecipientsCreated=se recipients exists limit1 return; else getRecipientsByFilter con org da creator profile org_id merge filter; generateRecipientTypes; createCommunicationRecipients; zero recipients→update total_recipients 0
    - scheduleCommunication=update status scheduled + scheduled_for ISO
    - sendResetPasswordEmail=supabase.auth.admin.generateLink recovery+redirectTo; variabile template `22club_reset_password_url`; sendEmailViaResendTemplate RESEND_TEMPLATE_PASSWORD_RESET ref=[[communications_context]] resend_template_reset
    - api_post_send=POST auth; ruoli consentiti admin|trainer|nutrizionista|massaggiatore|marketing; body JSON `communicationId`; preflight `communications` select session client id+type+recipient_filter+status+created_by_profile_id ref=RLS; type email|all; `ensureRecipientsCreated`+`sendCommunicationEmail` ref=lib scheduler+email admin; 500 se result.success false ref=[[communications_fetch]] api_get_list (stesso dominio ruoli list)
    - api_post_send_email_to_athlete=POST stesso set ruoli+`created_by_profile_id`; body athlete_id subject body athlete_name optional; resolve athlete `email` session; HTML minimale `<p>` per riga escape `<>`; `sendEmailViaResend` senza tracking id; insert session `communications` status sent totals 1 metadata to_email athlete_id+recipient_filter source email_to_athlete; poi insert `communication_recipients` sent; se insert fallisce→warn log ma `success:true` (email già inviata) ref=[[communications_context]] email_sent_db_catalog_split
    - api_post_check_stuck=POST auth; select `communications` id status sending eq created_by_profile_id self; update same filter→status failed; `{reset:N}`; errori→reset 0 body 200 ref=[[communications_context]] check_stuck_soft_response
    - ui_send_binding=use-communications sendCommunication→POST `/api/communications/send` body `{communicationId}` refresh lista dopo success ref=api_post_send
  - QUERIES
    - use=communications.insert select single
    - use=communications.update eq id
    - use=communications.delete eq id
    - use=communication_recipients.delete eq communication_id
    - use=communication_recipients.insert select (chunked)
    - use=communication_recipients.update eq id recipient
    - use=communications.select \* eq id single (sendCommunicationEmail)
    - use=communication_recipients.select \* eq communication_id eq recipient_type email eq status pending
    - use=communication_recipients.select \* join communications eq id recipient (sendEmailToRecipient)
    - use=profiles.select email eq user_id (sendEmailToRecipient)
    - use=profiles.select email eq id recipient_profile_id (batch)
    - use=communications.select \* eq status scheduled lte scheduled_for not null (scheduler)
    - use=communication_recipients.select id limit 1 eq communication_id (ensureRecipientsCreated)
    - use=profiles.select org_id eq id created_by_profile_id (ensureRecipientsCreated)
    - use=supabase.auth.admin.generateLink recovery (reset password)
    - use=communications.select id type recipient_filter status created_by_profile_id eq id single (api send preflight session)
    - use=communications.insert select id single (api send-email-to-athlete session)
    - use=communication_recipients.insert (api send-email-to-athlete session)
    - use=communications.select id eq status sending eq created_by_profile_id (api check-stuck)
    - use=communications.update status failed eq status sending eq created_by_profile_id (api check-stuck)
  - CONTEXT
    - name=sendEmailToRecipient_profile_key
    - issues=join path usa profiles.user_id per email; batch usa recipient_profile_id=id profilo—coerenza assunta ref=[[communications_fetch]] recipient_user_id_maps_profile_id
    - name=ui_send_response_parse
    - issues=use-communications sendCommunication legge response.text; guard empty/trim HTML prefix; JSON.parse catch→error UX ref=api_post_send
    - name=ui_client_crud_delta
    - issues=createCommunication updateCommunication deleteCommunication usano supabase browser+profiles.id da auth user_id (non service admin lib); insert/update cast any workaround types
    - name=ui_reset_draft_hook
    - issues=resetCommunication hook client update status draft where sending|failed—diverso da api_post_check_stuck che imposta failed ref=[[communications_context]] ui_reset_vs_api
