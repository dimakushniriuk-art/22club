- communications_modal
  - ATOMS
    - source_file=src/components/communications/new-communication-modal.tsx
    - source_file=src/components/communications/recipients-detail-modal.tsx
    - wiring_parent=src/app/dashboard/comunicazioni/comunicazioni-client.tsx
    - lazy_bundle=Suspense+StaffLazyChunkFallback su entrambi i modal
    - new_modal_props=isOpen onClose isEditing form\* recipientCount callbacks onCreateDraft onCreateAndSend
    - recipients_modal_props=isOpen onClose communicationId communicationTitle
    - formType_prop=onFormTypeChange esposto ma UI non muta tipo (prop `_onFormTypeChange` unused) state resta da parent
  - COMPRESSED
    - new_modal_dirty_guard=snapshotRef su primo open isOpen transition; isDirty confronta title message type recipientFilter scheduled date athletes[] vs snapshot; close→AlertDialog conferma o onClose
    - new_modal_recipients_ui=segmenti visibili `atleti`|`custom` (nessun bottone `all` in UI); count parent-side ref=[[communications_fetch]] api_get_recipients_count querystring da use-communications-page effect
    - new_modal_schedule=Checkbox Programma invio + datetime-local; payload scheduled_for passa da orchestration parent ref=[[communications_mutations]] createCommunication status scheduled vs draft
    - new_modal_footer=Salva bozza|Invia ora disabled se title|message vuoti o formLoading; entrambi chiamano stesso handler family lato parent (draft vs sendNow)
    - athlete_selector_embedded=mount fetch GET `/api/communications/list-athletes` ref=[[communications_fetch]] api_get_list_athletes; local state search filtra nome|email; checkbox toggle array id; clear all
    - recipients_modal_fetch=open effect GET `/api/communications/recipients?communication_id=` ref=[[communications_fetch]] api_get_recipients; close reset recipients statusFilter search error
    - recipients_modal_filters=client statusFilter all|pending|sent|delivered|opened|failed; search nome|email|telefono; stats aggregate su array raw
    - recipients_modal_error=banner error + Riprova refetch manuale
    - recipients_modal_table=colonne stato+error_message truncate 50px; sticky header scroll
  - QUERIES
    - use=(client) GET list-athletes JSON athletes[]
    - use=(client) GET recipients communication_id
  - CONTEXT
    - name=bounced_filter_gap
    - issues=UI filtro `failed` include solo status failed in tabella; stats.failed somma failed|bounced ma filtro button `failed` non mostra bounced isolati
