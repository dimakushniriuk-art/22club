- appointments_recurrence
  - ATOMS
    - APT.rec.type=RecurrenceType none|daily|weekly|monthly
    - APT.rec.cfg=interval default1|endDate YYYY-MM-DD|null|count|null|daysOfWeek[] 0-6 weekly
    - APT.rec.ser=serializeRecurrence: none→null else JSON.stringify{type,interval,endDate,count,daysOfWeek}
    - APT.rec.des=deserializeRecurrence JSON.parse catch→none defaults
    - APT.rec.genDates=generateRecurrenceDates(startDate,config,maxOccurrences default 100): loop occurrenceCount<maxCount maxCount=config.count||maxOccurrences; endDate break; daily|weekly(findNextDayOfWeek)|monthly branches
    - APT.rec.genApt=generateRecurringAppointments(baseStart,baseEnd,config): APT.rec.genDates→per date setHours da baseStart→ends base duration getAppointmentDuration
    - APT.rec.max=generateRecurrenceDates param maxOccurrences=100 default (appointment-modal comment max 100 occorrenze generateRecurrenceDates)
    - ref=[[appointments_calendar]] getRecurrenceSlots 2_weeks|1_month|6_months|1_year (use-calendar-page) distinto da recurrence-utils weekly product
    - ref=[[appointments_modal]] RecurrenceSelector+serialize su insert batch appointment-modal
  - COMPRESSED
    - recurrence-utils: cfg↔JSON↔date[]→generateRecurringAppointments applica stessa durata oraria su ogni data
    - calendar staff: altra funzione getRecurrenceSlots finestra fissa da primo ISO+step settimanale fino limite tempo
  - QUERIES
    - use=src/lib/recurrence-utils.ts#serializeRecurrence|deserializeRecurrence|generateRecurrenceDates|generateRecurringAppointments|getAppointmentDuration|DAYS_OF_WEEK|findNextDayOfWeek
  - CONTEXT
    - nome:doppia fonte ricorrenza
    - issues=recurrence-utils usato modal dashboard; use-calendar-page ha helper separati until_lessons e recWin
    - use=scegliere modulo in base entrypoint UI
