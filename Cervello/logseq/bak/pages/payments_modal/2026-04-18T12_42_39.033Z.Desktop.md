- payments_modal
  - ATOMS
    - source_file=src/components/dashboard/payment-form-modal.tsx
    - source_file=src/components/dashboard/nuovo-pagamento-modal.tsx
    - source_file=src/app/dashboard/pagamenti/atleta/[athleteId]/page.tsx
    - ledger_writes_client=addCreditFromPayment addReversalFromPayment insertManualCreditLedgerRow ref=[[payments_mutations]]
    - storage_bucket=documents (upload fatture)
    - rpc_org_fallback=get_org_id_for_current_user (nuovo modal + edit pagamento pagina)
    - rpc_ledger_edit=staff_update_credit_ledger_movement
    - rpc_ledger_delete=staff_delete_credit_ledger_movement
  - COMPRESSED
    - payment_form_modal=state PaymentFormData athlete_id amount payment_method contanti|bonifico|carta|paypal lessons_purchased notes; default athlete empty amount 0 method contanti lessons 0; useClienti→select atleta; validate athlete+amount>0+lessons>0; auth getUser; staff profiles id+user_id+role+org_id eq user_id; org_id=staff.org_id else organizations.select id limit1 maybeSingle→errore se ancora vuoto; serviceType=defaultServiceForRole(staff.role); insert payments status completed payment_date now ISO notes trim|null created_by_staff_id+created_by_profile_id staff.id org_id service_type; select id single; addCreditFromPayment; invalidatePaymentsQueries+invalidateClientiQueries; toast success; close reset; error inline+toast; loading→submit+annulla disabled; Dialog onOpenChange handleClose no reset se loading
    - nuovo_pagamento_modal=props serviceType ServiceType; defaultAthleteId optional; lockAthlete default true→SimpleSelect atleta disabled se default+lock; open effect loadAthletes profiles role athlete order nome; reset form payment_date oggi lessons 1 amount 0 invoice null; initialFormRef dirty; handleOpenChange dirty→ConfirmDialog altrimenti reset+close; file invoice PDF only max 10MB else error; FileReader preview data URL; validate athlete+date+lessons>=1+amount>0 array log; getUser+getSession; staff profiles id user_id (no org in select cast org_id optional); verify athlete profiles id single; payment_method fisso abbonamento notes template Abbondamento N allenamenti; org cast staff org_id else supabase.rpc get_org_id_for_current_user; insert payments payload created_by_profile_id org_id; post-insert upload optional path fatture/{serviceType}/{athlete_id}/{paymentId}.{ext} documents upsert false; update payments invoice_url; upload error→setError return senza proseguire (pagamento già in DB); addCreditFromPayment; invalidatePaymentsQueries+invalidateClientiQueries; loading+uploading block close handleClose
    - pagamenti_atleta_embedded=route serviceType da parseServiceFromUrl(searchParams) default training; NuovoPagamentoModal open newPaymentOpen serviceType defaultAthleteId=athleteId lockAthlete onSuccess load; tabella pagamenti azioni Modifica→openEditPayment saveEditedPaymentAsHistory=cancel payments update status cancelled+notes timestamp staff; addReversalFromPayment(oldPayment,staffId) ref=[[payments_mutations]]; insert nuovo payments correction notes payment_method abbonamento org via profiles.org_id o rpc get_org_id_for_current_user; invoice file optional upload nuovo id path fatture/{serviceType}/{athlete}/{newId}; addCreditFromPayment nuovo; Elimina→soft delete payments deleted_at deleted_by_profile_id; invoice-only upload hidden input upsert true path stesso pattern DOCUMENTS_STORAGE_BUCKET; canAttachInvoiceOnly=!invoice && !is_reversal && status!==cancelled; bottone Storno UI disabled title Gestione Pagamenti (non usePayments.reversePayment)
    - pagamenti_atleta_ledger_dialogs=create insertManualCreditLedgerRow validazioni qty segno per entry_type UUID payment_id appointment_id; edit saveLedgerMovementEdit rpc staff_update_credit_ledger_movement p_id p_athlete_id p_service_type p_entry_type p_qty p_reason p_created_at; delete confirm staff_delete_credit_ledger_movement p_id p_athlete_id p_service_type; load() refresh dopo mutazioni
  - QUERIES
    - use=profiles.select (vari: atleta header, staff, lista atleti role athlete)
    - use=payments.select|insert|update (pagina + modal)
    - use=organizations.select id limit 1 (solo payment-form-modal org fallback)
    - use=rpc.get_org_id_for_current_user (nuovo modal + edit pagina)
    - use=storage.from(documents).upload
    - use=rpc.staff_update_credit_ledger_movement
    - use=rpc.staff_delete_credit_ledger_movement
  - CONTEXT
    - name=invoice_path_convention
    - issues=fatture/{service_type}/{athlete_id}/{paymentId}.{ext} allineato tra nuovo modal e pagina; nuovo upsert false vs invoice-only/edit upsert true dove indicato in codice
    - name=ledger_ui_vs_hook_reversal
    - issues=correzione pagamento usa REVERSAL ledger lib; lista generica usePayments.reversePayment ancora ref=[[payments_mutations]] hook_payments_row_reversal
