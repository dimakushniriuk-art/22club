- payments_context
	- ATOMS
		- source_file=src/lib/credits/is-athlete-assigned-to-trainer-profile-admin.ts
		- tables=athlete_trainer_assignments pt_atleti
		- hook_scope=src/hooks/use-payments-stats.ts
		- cache=statsCache ref=@/lib/cache/cache-strategies
	- COMPRESSED
		- isAthleteAssignedToTrainerProfileIdAdmin=UUID lowercase; active assignment OPPURE pt_atleti match; commento=allineamento RPC is_athlete_assigned_to_trainer per ruoli non coperti da get_current_trainer_profile_id
		- usePaymentsStats=useRPC default true; TTL cache interno a statsCache; fallback client solo se array payments passato; month hook +1 vs Date.getMonth in filter fallback
	- QUERIES
		- use=athlete_trainer_assignments.select(id).eq(status,active)
		- use=pt_atleti.select(pt_id)
	- CONTEXT
		- name=scope_boundary
		- issues=gating accessi debit/credit non è economia ledger; ref dominio assignments/atleti fuori cartella credits
		- ref=[[payments_mutations]] (coached debit server route usa contesto simile non in questo file)
		- name=staff_vs_athlete_list_partition
		- issues=trainer/admin listano solo pagamenti con created_by_staff_id=userId; athlete solo athlete_id — rischio “pagamenti creati da altro staff” invisibili al PT
		- name=stats_surface_divergence
		- issues=getStats (usePayments) mese 0-based e dataset già filtrato lista vs RPC mensile 1-based + possibile cache stale tra schermate
		- name=soft_delete_visibility
		- issues=includeDeleted default false; solo flag prop hook non enforcement lato tipo
