- analytics_context
	- ATOMS
		- source_file=src/lib/analytics.ts
		- multi_tenant_note=comment orgId futuro; orgId già su cache key + parziale query trend
		- duckdb_note=comment integrazione futura docs/duckdb-integration-evaluation.md
		- rpc_typing=supabase.rpc cast any per distribution+performance
		- athlete_id_ambiguity=comment vista athlete_id≈profiles.id vs workout_logs atleta_id|athlete_id dual match client filter
		- performance_fallback_chain=RPC → view+logs → profiles+logs+plans
		- distribution_fallback=RPC fail→[] (no mock)
		- global_fallback_getAnalyticsData=catch logger.error→trend/distribution/performance [] summary zeros
		- getTrendData_slice_warning=getTrendData(orgId,days) usa getAnalyticsData poi slice(0,days) su trend ordinato ascending=primi giorni più vecchi non ultimi
		- calculateGrowthMetrics_order_bug=trend[0]=primo elemento dopo sort ascending (=giorno più vecchio) trend[1]=secondo giorno; confronto “current vs previous” semanticamente invertito rispetto a “ultimi due giorni”
		- source_file_trainer=src/lib/trainer-analytics.ts
		- header_trainer_note=churn/retention su athlete_trainer_assignments sono proxy solo date
		- trainer_id_normalization=trim toLowerCase su match trainerSet/roster
		- magic_rph_alert_threshold=revenuePerHour<25→yellow se >0; revenuePerHour<=0 e revenueTotal>0→red
		- heuristic_insights_constants=aderenza<65 copy; churn>=5 copy; payment mix top>=75% dipendenza singolo tipo; max 6 righe
		- utilizationNote_static=testo heatmap senza capacità slot agenda
		- source_file_export=src/lib/analytics-export.ts
		- csv_deprecated_side_effect=exportToCSV download immediato browser (legacy)
		- source_route=src/app/dashboard/statistiche/page.tsx
		- source_shell=src/components/dashboard/statistiche-page-content.tsx
		- source_trainer_ui=src/components/dashboard/trainer-statistiche-dashboard.tsx
		- source_legacy_only=src/components/dashboard/statistiche-content.tsx
		- export_button=src/components/dashboard/export-report-button.tsx
		- growth_page_semantics=calculateGrowthMetrics(page) prima metà vs seconda metà trend asc (≠ lib calculateGrowthMetrics primi due punti ref=[[analytics_fetch]] export_calculateGrowthMetrics)
		- trainer_ids_trainer_role=selectedTrainerIds=[user.id] useAuth user
		- trainer_ids_admin_role=fetchOrgTrainerOptions ref=[[analytics_fetch]] export_fetchOrgTrainerOptions; default all; toggle min 1
		- trainer_fetch_params=fetchTrainerAnalyticsReport {orgId,trainerIds,startBoundary,endBoundary} boundariesForPeriod(rangeDays)
		- legacy_fetch_gate=shouldLoadLegacy=!authLoading&&(!trainerMode||role==='admin'); trainerMode=trainer|admin
		- default_active_tab=trainer; admin tab Trainer|Organizzazione legacy
		- refresh_aggregate=isRefreshing=authLoading||trainerTabWaiting||legacyTabWaiting
		- export_surface=ExportReportButton legacyData sempre; trainerReport se showTrainerPanel; truthy trainerReport→PDF trainer ref=[[analytics_mutations]]
		- chart_props_TrendData=TrendChart|MultiTrendChart data=legacyData.trend; default title "15 giorni" stale vs range
		- chart_props_DistributionData=DistributionChart|HorizontalBarChart|VerticalBarChart; Pie dataKey=count nameKey=type
		- kpi_props=KPIMetrics summary+legacyGrowth; growth 0→icona neutra (!growth falsy)
		- performance_top5=PerformanceMetrics sort completion_rate in-place side-effect
		- trainer_dashboard_charts=client-recharts+analytics-chart-theme; buildWeeklyRollupFromDaily se week
		- trainer_local_filters=heatmapTz utc|rome; heatmapWorkdaysOnly; trendMode revenue_hours|adherence; timeGranularity day|week se daily.length>7
		- trainer_links=/dashboard/atleti/{id}|pagamenti/atleta|calendario?athlete=
	- COMPRESSED
		- logging=logger.warn su errori parziali workout_logs|documents|appointments|RPC|view|athletes; logger.error su catch trend|distribution|performance|getAnalyticsData
		- unstable_cache=cookies read fuori callback; createClient dentro callback cached
		- getPerformanceData_fallback_profiles=completion da is_active workout_plans (inactive=completed proxy) non stesso modello RPC/view
		- trainer_empty_fallback=fetchTrainerAnalyticsReport catch o guard org/trainer→aggregate con dataset vuoti (report strutturato ma KPI zerati)
		- includePeriodComparison_cost=doppio loadTrainerAnalyticsAggregateInput+aggregate finestre current+previous
		- appointments_or_filter=dynamic staff_id|trainer_id in join trainerIds (UUID attesi)
		- heatmap_tz_duality=UTC vs Europe/Rome Intl formatToParts en-US short weekday
		- adherence_trainer=eseguiti/(prenotati+eseguiti+annullati+cancellati) solo appuntamenti tipo allenamento classificati
		- no_show_union=status no_show|assente su ids workout-apt OR appointment_cancellations cancellation_type variants noshow
		- progress_weight_pct=primo vs ultimo peso periodo; progressWeightAvgPct media atleti con>=2 misure; compositeScore 0.4/0.3/0.3
		- revenueGrowthHalf=meta vs meta revenue su daily
		- page_orchestration=state period+tabs+legacy+trainer+options+loadings; effects trainer list|legacy client ref=[[analytics_fetch]] dashboard_client_legacy|trainer report
		- shell_visibility=trainerPanel authReady&&trainerMode&&tab trainer; legacyPanel !trainerMode||admin&&tab legacy; skeleton !authReady
		- admin_multi_trainer=details checkbox se trainerOptions.length>1
		- hints_trainer_panel=no_org|no_trainers|none_selected
	- QUERIES
		- use=N/A lib transform ref=[[analytics_fetch]]; UI fetch client ref=[[analytics_fetch]] dashboard_client_legacy; trainer fetchTrainerAnalyticsReport
	- CONTEXT
		- name=growth_metrics_vs_trend_order
		- issues=calculateGrowthMetrics assume trend[0] recente ma getTrendDataFromDB sort ascending → KPI growth probabilmente errati o fuori intento
		- use=verificare consumer UI prima di cambiare sort o indicizzazione
	- CONTEXT
		- name=cache_invalidation
		- issues=tag analytics revalidate 300s; orgId nel key; invalidazione granulare non definita in questo file
	- CONTEXT
		- name=query_load_performance_path
		- issues=fallback profiles carica workout_logs senza filtro DB su atleta (select all completati poi filter client) → costo potenziale alto se molti log
		- ref=[[analytics_fetch]]
	- CONTEXT
		- name=log_daykey_vs_appointment_iso
		- issues=workout_logs filtro `data` string YYYY-MM-DD; appointments `starts_at` ISO range; possibile skew TZ vs ref=[[analytics_fetch]] trend engine analytics.ts
	- CONTEXT
		- name=lesson_usage_dependency
		- issues=lessonUsageByAthleteIds fallisce→warn log usage vuoti su lesson balance rows
		- ref=[[payments_fetch]]
	- CONTEXT
		- name=export_pdf_data_contract
		- issues=PDF assume dati già coerenti; nessuna validazione extra; ref=[[analytics_mutations]]
	- CONTEXT
		- name=auth_role_flash_guard
		- issues=comment page: senza gate role null si fetchava legacy prima del ruolo→flash; shouldLoadLegacy+authReady in shell
		- use=page.tsx shouldLoadLegacy+StatistichePageContent authReady
	- CONTEXT
		- name=timezone_daykey_client
		- issues=boundary Date locale + toISOString dayKey vs classify appointment starts_at toISO dayKey; tick TrendChart new Date(day) it-IT (interpretazione UTC midnight)
		- use=ref=[[analytics_fetch]] log_daykey_vs_appointment_iso; dashboard_client_legacy
	- CONTEXT
		- name=trainer_report_scope
		- issues=fetchTrainerAnalyticsReport page non passa includePeriodComparison/thresholds→default lib; KPI heatmap duplicate UTC+Rome già in report
		- ref=[[analytics_fetch]] trainer_period_compare optional
	- CONTEXT
		- name=performance_metrics_mutation
		- issues=useMemo [performance] ma sort in-place su prop→rischio reorder dati legacy condivisi
		- use=src/components/shared/analytics/kpi-metrics.tsx PerformanceMetrics
	- CONTEXT
		- name=statistiche_content_duplication
		- issues=statistiche-content.tsx duplica wiring legacy+Export vs StatistichePageContent legacy panel; route attuale usa solo StatistichePageContent
		- use=entry alternativo o dead import check
