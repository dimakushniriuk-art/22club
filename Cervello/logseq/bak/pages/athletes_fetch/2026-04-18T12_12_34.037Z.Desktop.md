- athletes_fetch
  - ATOMS
    - folder=src/hooks/athlete-profile
    - pattern=useQuery+queryKeyFactory per tab profilo
    - athlete_id_param=user_id (UUID auth user dell’atleta) nei hook tab
    - profile_id=useAthleteProfileData usa profiles.id (role=athlete) + profiles.user_id separato
    - rq_stale_default_ms=60000
    - rq_gc_default_ms=900000
    - rq_retry_default=1
    - ai_data_stale_ms=300000
    - ai_data_gc_ms=3600000
    - tab_prefetch_stale_ms=60000
    - cache_layer=file=src/lib/cache/cache-strategies athleteProfileCache via useAthleteProfileCache
    - rq_invalidate_key_prefix=athlete-profile
    - rq_invalidate_complete_key=athlete-profile-complete
    - progress_scope=file=src/lib/progress-logs-athlete-scope.ts progressLogsAthleteIdOrFilter(athleteId,athleteUserId)
  - ATOMS.ROUTE.delta
    - ref=[[athletes_fetch]] logica query tab invariata
    - staff_route_param=params.id|semantic=profiles.id|first_consumer=useAthleteProfileData(.eq id)
    - tab_ui_prop_athleteId=profiles.user_id|wired=AthleteProfileTabs→Athlete\*Tab athleteId={athleteUserId}|contrast route athleteId prop=profiles.id per progressi/documenti
    - foto_binding_risk=file=src/app/dashboard/atleti/[id]/progressi/foto/page.tsx|useProgressPhotos({userId:id,role:athlete})|id=profiles.id|hook filters eq athlete_id=userId→allineare a athleteUserId se colonna athlete_id=user_id
    - foto_binding_verified_code=file=src/app/dashboard/atleti/[id]/progressi/foto/page.tsx|params.id→const id string|null|useAthleteProfileData(id) PK profiles|useProgressPhotos userId=id|role prop literal string 'athlete'|non passa useAuth().role
    - foto_hook_mapping=file=src/hooks/use-progress-photos.ts|role===athlete&&userId→query.eq athlete_id userId|else staff→nessun eq athlete_id|select PROGRESS_PHOTOS_LIST_COLUMNS|PHOTOS_PER_PAGE=20|order date desc+created_at desc|range page|hasMore=fetched.length===20|frequentQueryCache key progress-photos:${userId}:${page}|angle:|date: optional
    - foto_page_coupling=StaffAtletaProgressiFotoPage|useEffect filterByAngle(selectedAngle) dopo cambio angolo|useMemo client filter photos stesso angle+sort date desc|IntersectionObserver loadMore|trainer early replace `/dashboard/atleti/${id}?tab=progressi`
  - QUERIES.ROUTE.delta
    - use=audit binding staff `/dashboard/atleti/[id]` dopo fix foto|use=non confondere route id con arg tab profilo
    - use=foto staff|fix minimo userId=athlete.user_id da useAthleteProfileData state|role reale useAuth o staff branch coerente
  - ATOMS.LIB
    - progress_photos_cols=file=src/lib/progress/progress-photos-columns.ts|export=PROGRESS_PHOTOS_LIST_COLUMNS|string literal select list|cols=id,athlete_id,date,angle,image_url,note,created_at,updated_at,session_saved_at|contract=align ProgressPhoto src/types/progress.ts
    - misurazioni_lib=file=src/lib/progressi/misurazione-progress-log-row.ts|MISURAZIONE_FIELD_TO_DB UI misurazioneField→progress_logs column|aliases torace_cm→chest_cm|vita_cm→waist_cm|fianchi_cm→hips_cm|coscia_media_cm reads log.coscia_media_cm??log.thighs_cm|fns=getProgressLogsDbColumnForMisurazioneField|readMisurazioneValueFromLogRow|buildMisurazioneListItemsFromProgressLogs filters UUID log.id+numeric value+sort desc date|progressLogListItemsToChartHistory asc date|toProgressLogMeasurementRow merges optional nulls into ProgressLogMeasurementRow|ref_hook_rows=[[athletes_fetch]] COMPRESSED.PROG use-progress-analytics progressLogRows consumer
    - body*metric_trends=file=src/lib/body-metrics/body-metric-trend-rules.ts|BODY_METRIC_TREND_RULES per misurazioneField+workout_stat*\*|rules decrease_is_good|increase_is_good|neutral|getBodyMetricTrendRule unknown→neutral|getBodyMetricDeltaSentiment(delta%) delta null|NaN|0→neutral|decrease_is_good negative delta→success positive→danger|inverse for increase_is_good|bodyMetricDeltaSentimentTextColorClass tailwind text-emerald|text-red|text-text-primary|doc=chiavi allineate misurazioni-values-content progress_logs
    - clienti_data=file=src/lib/clienti/fetch-clienti-data.ts|exports fetchClientiStats|fetchClientiList|logger=fetchClienti|isSupabaseConfigured guards mock URL/anon key
  - COMPRESSED.LIB
    - fetchClientiStats=!isSupabaseConfigured→fixed demo ClienteStats|else RPC get_clienti_stats Promise.race 3000ms→localStorageCache key clienti-stats TTL=120000ms|catch→parallel head estimated counts profiles role=athlete + stato attivo|inattivo + nuovi_mese gte firstDayOfMonth toISOString + documenti_scadenza each queryWithTimeout 2000ms→statsCache set|throw path→emptyStats
    - fetchClientiList=!userId&&!skipAuthCheck→{clienti:[],total:0}|!configured→mock Cliente[]+client filters|else profiles select fixed columns eq athlete order data_iscrizione ascending=sort.direction only **limit pageSize senza range DB**|in-memory filters stato|search trim|solo_documenti_scadenza|date da|a su data_iscrizione??created_at|client sort if sort.field!==data_iscrizione|slice page window|frequentQueryCache clienti-list=filtered full|query Promise.race 30000ms timeout→reuse clienti-list cache|total page1 count head estimated +filters +search ilike if len>2 race 3000ms|fallback total heuristic len===pageSize→len\*5 else len|map Cliente allenamenti_mese=0 scheda_attiva=null tags=[]|post-filter allenamenti_min|tags su mapped (allenamenti sempre 0→filtro min inefficace)
  - QUERIES.LIB
    - use=select progress_photos columns contract|use=mappa campi misurazioni↔DB progress_logs|use=sentiment freccia/variazione metriche|use=origine lista+stats clienti dashboard lista atleti|use=debug totali stima vs RLS window fetch
  - COMPRESSED
    - useAthleteProfileData: profiles select+map Cliente; stats parallele workout_logs count OR atleta_id|athlete_id, mese gte startOfMonthLocalYmd, workout_plans active, documents in_scadenza, progress_logs weight latest, lesson_counters; duplicate retry stesso query su profileError
    - useAthleteTabPrefetch: prefetchQuery per tab UI→supabase allineato a keys anagrafica/medica/fitness/motivazionale/nutrizione/massaggio/amministrativo/smart-tracking/ai-data
    - useAthleteProfileCache: get/set/invalidate/prefetch sezione; invalida anche queryKey athlete-profile|section
    - Tab data hooks: useAthleteAnagrafica profiles; useAthleteMedical athlete_medical_data maybeSingle; useAthleteFitness/Motivational maybeSingle; useAthleteNutrition/Massage/Administrative single con PGRST116→null dove usato; useAthleteSmartTracking ultimo per data_rilevazione; useAthleteSmartTrackingByDate eq data; useAthleteAIData ultimo per data_analisi
  - QUERIES
    - use=leggere dati tab profilo e statistiche header dashboard atleta
    - use=prefetch on hover tab tramite handlePrefetchTab(athleteUserId)
    - use=cache locale profilo sezione+bridge React Query invalidate
    - use=progressi KPI/storico/foto paginate/reminder read-side (delta sotto)
  - ATOMS.PROG.delta
    - PROG.paths=src/hooks/use-progress.ts|use-progress-optimized.ts|use-progress-analytics.ts|use-progress-photos.ts|use-progress-data.ts|use-progress-reminders.ts
    - PROG.photos_select=file=src/lib/progress/progress-photos-columns.ts PROGRESS_PHOTOS_LIST_COLUMNS
    - PROG.stats_rpc=get_progress_stats athlete_uuid=userId
    - PROG.stats_cache=file=src/lib/cache/cache-strategies.ts statsCache key=progress-stats:${userId}
    - PROG.photos_cache=frequentQueryCache key prefix progress-photos:${userId}:${page} angle|date suffix
    - PROG.rq.key=progress-analytics|athleteId
    - PROG.rq.stale_ms=10000
    - PROG.rq.refetchOnMount=always
    - PROG.rq.refetchOnWindowFocus=false
    - PROG.rq.gc_ms=300000
    - PROG.photos.page_size=20
    - PROG.profile_id_bridge=file=src/lib/utils/profile-id-utils.ts getProfileIdFromUserId per workout_plans.athlete_id in analytics
    - PROG.measurement_rows=file=src/lib/progressi/misurazione-progress-log-row.ts toProgressLogMeasurementRow
    - PROG.data_hook=useProgressData derive_locale su progressLogs prop nessuna rete
  - COMPRESSED.PROG
    - use-progress: state fetch progress_logs|progress_photos order date desc; athlete+userId→eq athlete_id; admin|trainer+userId→query senza eq (RLS); getProgressStats RPC→statsCache else fallback 2 logs+count photos; getLatestProgressLog; helper client getPhotosForDate|hasPhotosForDate|getWeightChange|getStrengthChange
    - use-progress-optimized: solo getProgressStats RPC o fallback manuale identico use-progress fallback
    - use-progress-analytics: useQuery fetchProgressData supabase.auth.getSession gate log; progress_logs select esteso limit 100 order date+created_at desc; errore query→KPI empty non throw; workout_plans last30d limit 50 is_active usato in KPI completamento/streak; useRealtimeChannel progress_logs|workout_plans→invalidate PROG.rq.key
    - use-progress-photos: range paginazione hasMore=len===20; athlete+userId→eq athlete_id; staff nessun eq; angle|dateFilter; frequentQueryCache per page|filterByAngle|filterByDate azzerano currentPage e invalidano frequentQueryCache pagine 0..currentPage|refresh stesso pattern poi fetchPhotos(0,false)
    - use-progress-reminders: last date progress_logs|progress_photos single; early-return se role!==atleta (vedi [[athletes_context]] PROG.reminder_role_mismatch)
  - QUERIES.PROG
    - use=dashboard progressi grafici analytics trainer/atleta
    - use=galleria foto progressi filtri angolo/data|use=verificare userId vs athlete_id FK prima di tuning cache foto
    - use=card trend peso da logs già fetchati altrove
    - use=dettaglio trend color↔[[athletes_fetch]] ATOMS.LIB body_metric_trends
  - CONTEXT
    - name=foto-binding-operativo
    - issues=ref [[athletes_context]] CONTEXT name=dashboard-atleti-id-binding|issues=PROG.foto_id_mismatch_operativo
    - use=ATOMS.ROUTE.delta foto_binding_verified_code|foto_hook_mapping
