- chat_context
	- ATOMS
		- source_file=src/hooks/chat/use-chat-conversations.ts
		- logger=ref=@/lib/logger createLogger hooks:chat:use-chat-conversations
		- cache_impl=ref=@/lib/cache/cache-strategies frequentQueryCache TTL implicit strategia frequent-query
		- hook_contract=callback injection onSuccess|onError no internal React state list
	- COMPRESSED
		- cache_swr_behavior=hit non-vuoto→onSuccess stale poi fetch completo→set cache+onSuccess ref=[[chat_fetch]] cache_key_pattern
		- role_branching=STAFF_ROLES admin|trainer→merge tutti atleti org+staff stessa org; athlete→PT attivo+staff_atleti attivi merge senza bloccare lista
		- silent_degrade=catch athlete PT fetch→logger.warn non throw; catch staff_atleti block→logger.warn ref=[[chat_fetch]] athlete_expand_pt athlete_expand_linked_staff
		- ordering_risk=sort solo last_message_at sintetico/updated_at fallback può mescolare thread reali vs placeholder ref=[[chat_fetch]] finalize
		- debug_surface=logger.debug RPC request/response slice 3 + conversations loaded summary
		- onError_contract=catch finale→onError string Error.message o literal Errore nel caricamento delle conversazioni L512-L514
	- QUERIES
		- use=(none oltre ref=[[chat_fetch]] QUERIES)
	- CONTEXT
		- name=rpc_vs_fallback_parity
		- issues=RPC fornisce unread_count e last_message_at curati; fallback+merge staff può divergere conteggi e ordering ref=[[chat_fetch]] fallback_unread_zero
		- name=profile_id_source
		- issues=getCurrentProfileId async esterno hook—auth user vs profileId devono restare allineati ref=[[chat_fetch]] auth_gate
		- name=staff_list_scope
		- issues=staff vede tutti atleti role=athlete filtro org se org_id presente—carico lista ampio lato client ref=[[chat_fetch]] staff_expand_athletes
		- name=placeholder_identity
		- issues=voci senza messaggi usano date sentinel e nomi da profilo/email—UX lista “conversazioni” include contatti mai chattati ref=[[chat_fetch]] staff_expand_athletes
		- name=client_rls_surface
		- issues=fetch solo supabase client—visibilità righe dipende da RLS/migrazioni non definite in questo hook
		- name=rpc_auth_param_semantics
		- issues=arg RPC user_uuid = auth.users.id; orchestrazione lista usa profileId ovunque altrove—contratto RPC↔DB deve garantire equivalenza ref=[[chat_fetch]] rpc_args
	- delta_realtime
		- ATOMS
			- source_legacy=src/hooks/chat/use-chat-realtime.ts
			- source_optimized=src/hooks/chat/use-chat-realtime-optimized.ts
			- dep_optimized=useChatProfile getCurrentProfileId ref=src/hooks/chat/use-chat-profile.ts
			- logger_optimized=ref=@/lib/logger createLogger useChatRealtimeOptimized
			- channel_legacy=fixed string chat_realtime
			- channel_optimized=dynamic chat_realtime_${profileId}
			- table_filter=public chat_messages postgres_changes
			- filter_receiver_insert_update=receiver_id=eq.${boundId}
			- filter_delete_receiver=DELETE receiver_id=eq.${profileId} optimized only
			- filter_delete_sender=DELETE sender_id=eq.${profileId} optimized only
			- bound_id_legacy=auth.users.id from supabase.auth.getUser().user.id
			- bound_id_optimized=profiles.id from getCurrentProfileId
			- callbacks=onMessageReceived onMessageUpdated; optimized optional onMessageDeleted
			- guard=isMounted legacy boolean; isMountedRef+channelRef optimized
		- COMPRESSED
			- legacy_handlers=INSERT+UPDATE receiver filter→debounced parent refresh via callbacks
			- optimized_handlers=INSERT+UPDATE receiver; DELETE both receiver and sender branches→onMessageDeleted
			- optimized_resubscribe=if channelRef exists→removeChannel await then new channel subscribe; cleanup effect→removeChannel channelRef null
			- legacy_effect_cleanup=outer useEffect return solo isMounted=false—return interno async IIFE non è cleanup React→removeChannel dentro IIFE return probabile non eseguito come unsub effect ref=CONTEXT legacy_channel_cleanup_gap
			- identity_binding=commento optimized: receiver_id tabella = profiles.id non auth.users.id—legacy filter su user.id rischio mismatch se DB usa profile UUID
			- fetch_empty_vs_realtime=ref=[[chat_fetch]] delta_messages merge_refresh_offset0 evita wipe lista su refresh dopo eventi realtime
		- QUERIES
			- use=supabase.auth.getUser
			- use=getCurrentProfileId async optimized path
		- CONTEXT
			- name=legacy_channel_cleanup_gap
			- issues=subscribe dentro async IIFE; removeChannel nel return dell’IIFE non collegato a useEffect cleanup esterno—rischio leak/doppi canali se dipendenze cambiano
			- name=realtime_identity_mismatch
			- issues=legacy filtra receiver_id=user.id optimized receiver_id=profileId—se schema chat usa profile id eventi legacy possono non arrivare o filtrare male
			- name=delete_event_scope
			- issues=solo optimized ascolta DELETE mittente e destinatario; mittente cancella messaggio→altro lato riceve DELETE sender_id branch
	- delta_ui_routes
		- ATOMS
			- logger_dashboard=ref=@/lib/logger createLogger app:dashboard:chat:page
			- logger_home=ref=@/lib/logger createLogger app:home:chat:page
			- hook_shell=useChat ref=src/hooks/use-chat.ts consumer-only
			- guard_chat_staff=useChatPageGuard ref=src/hooks/use-chat-page-guard.ts permission_key=staff_dashboard_home requirePermission redirect getRedirectPath kind=guard_staff_wrong_area showLoader
			- hidden_list_state=useChatHiddenConversations ref=src/hooks/use-chat-hidden-conversations.ts storage_key=chat-hidden-${profileId} json Set restore ?with= delete hidden+setSelected+setCurrentConversation processedWithParamRef
			- url_param_with=searchParams.get('with') router.replace scroll false basePath default=/dashboard/chat export ChatPageContent prop
			- storage_last_open=localStorage key=chat-last-with persist on withParam auto router.replace ?with unless userClearedListRef|error|initial loading guard
			- mobile_shell=matchMedia max-width 851px showListOnly|showConversationOnly sticky input footer staff
			- staff_selection=selectedConversationId handleSelectConversation setCurrentConversation+router; messagesForSelected filter other_user_id match + sender|receiver in thread ids
			- staff_remove_row=onRemoveFromList hidden Set+maybe clear selection
			- draft_leave_confirm=ConfirmDialog hasMessageDraft pendingBackAction list|browser ref=[[chat_modal]] draft_signal
			- staff_theme=CHAT_THEME_CLASSES role nutrizionista→teal else default underline
			- staff_placeholder_map=PLACEHOLDER_BY_ROLE massaggiatore|nutrizionista vs default consiglio motivazionale
			- staff_header_subtitle=role nutrizionista|massaggiatore vs atleti copy
			- staff_error_retry=banner error router.refresh vs full skeleton initial
			- home_auth_gate=authLoading ChatLoadingFullPage; !user|!isValidProfile empty main redirect note layout
			- home_identity_filter=currentUserId=user.id UUID isValidUUID; messagesForThisConversation pairwise sender/receiver vs otherIdForFilter
			- home_recipients_source=conversations map split nome|cognome role coerce trainer|nutrizionista|massaggiatore OR personalTrainer fallback singleton
			- home_auto_select=hasAutoSelectedRef+refs; priority ptConv personalTrainer.id then trainer role else first conv/personalTrainer id setCurrentConversation catch reset flag
			- home_bootstrap_fetch=hasFetchedEmptyRef recipientIdToFetch messagesCount 0→setCurrentConversation once per id
			- home_effective_conversation=synthetic participant isLoading true messages empty selectedRecipient|personalTrainer bridge
			- home_topbar=AthleteTopBarContext setConfig title subtitle onBack secondaryRow ref pattern recipientIdsKey deps cleanup null
			- home_send_guard=isValidMessageType validateNonEmptyString text notifyError ref=@/lib/notifications
			- home_delete_wire=MessageList onDeleteMessage=deleteMessage useChat
			- home_error_retry=ChatErrorState onRetry=fetchConversations
			- prefetch_staff=dynamic import('@/components/chat') effect
		- COMPRESSED
			- staff_two_panel_desktop=ConversationList+MessageList+MessageInput split; mobile single view back min-h 44
			- staff_loading_initial=isLoading conversations.length===0→StaffDashboardSegmentSkeleton
			- staff_thread_states=selected+participant match→list+input; selected mismatch→ChatEmptyState loading; none→select variant
			- home_footer_shell=MessageInput safe-area gradient border; disabled path quando !effectiveConversation
			- home_message_list_key=key=other_user_id remount su cambio destinatario
			- cross_ref_lists=conversations unread totals staff badge sum visibleConversations ref=[[chat_fetch]] rpc primary unread semantics
		- QUERIES
			- use=ref=[[chat_fetch]] delta_ui_page_fetches QUERIES page-level only
		- CONTEXT
			- name=use_chat_internals_out_of_scope
			- issues=useChat state machine non estratta in questo delta—solo binding pagina ref=src/hooks/use-chat.ts
			- name=staff_athlete_ux_split
			- issues=due entrypoint UX URL deep-link+lista vs auto-select+topbar integrato—stessa tabella messaggi vincoli filtro diversi
			- name=hidden_vs_server_truth
			- issues=hiddenConversationIds solo client localStorage—non sync server-side delete conversazione
